{{!-- <div class="d-flex justify-content-center">
  <div class="card mb-3 w-100">
    <div class="row g-0">
      <div class="col-md-4 d-flex align-item-center justify-content-center" style="height: 400px;">
        <img src="{{ viewedUser.image }}" style="height: 100%; width: 100%; object-fit:cover;" alt="user-image">
      </div>
      <div class="col-md-8 p-3">
        <div class="card-body">
          <h3 class="card-title">{{ viewedUser.name }}</h3>
          <p class="card-text">{{ viewedUser.email }}</p>
          {{#ifCond user.id viewedUser.id}}
          <a href="/users/{{ viewedUser.id }}/edit" class="btn btn-primary">Edit</a>
          {{/ifCond}}
        </div>
      </div>
    </div>
  </div>
</div>
</div>
<div class="card">
  <div class="card-header">
    <b>{{ comments.count }}</b> 已評論過的餐廳
  </div>
  <div class="row card-body">
    {{#each comments.rows}}
    <div class="col-2 p-0 m-2" style="width: 80px; height: 80px;">
      <a href="/restaurants/{{this.Restaurant.id}}"><img src="{{this.Restaurant.image}}"
          style="height: 100%; width: 100%; object-fit:cover;" alt="restaurant-image"></a>
    </div>
    {{/each}}
  </div>
</div> --}}
<div class="container">
  <div class="card d-flex flex-wrap flex-row p-0">
    <div>
      {{#if userProfile.image}}
      <img src="{{ userProfile.image }}" alt="avatar" class="rounded-circle m-3"
        style="object-fit:cover;height:300px; width:300px">
      {{else}}
      <img src="https://memeprod.sgp1.digitaloceanspaces.com/user-wtf/1588859390614.jpg" alt="avatar"
        class="rounded-circle m-3" style="object-fit:cover;height:300px; width:300px">
      {{/if}}
    </div>
    <div class="m-5" style="width:300px">
      <h5>{{ userProfile.name }}</h5>
      <p class="mt-2">{{userProfile.email}}</p>
      <p class="mt-4"><strong>{{comments.length}}</strong> 已評論餐廳</p>
      <p class="mt-1"><strong>{{userProfile.FavoritedRestaurants.length}}</strong> 已收藏的餐廳</p>
      <p class="mt-1"><strong>{{userProfile.Followings.length}}</strong> followings(追蹤者)</p>
      <p class="mt-1"><strong>{{userProfile.Followers.length}}</strong> followers(追隨者)</p>
      {{#ifCond userProfile.id user.id}}
      <a href="/users/{{user.id}}/edit" class="btn btn-primary">edit</a>
      {{/ifCond}}
      </div>
    </div>